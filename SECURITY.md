# セキュリティ

## 認証とローカル実行

アプリは、このPCにインストールしたCodex CLIを子プロセスとして起動する。ログインはCLIで行い、アプリは認証ファイルの読み取り・複製・保存を行わない。ChatGPTでのログイン状態は `codex login status` で確認できる。

個人の `config.toml` を読み込まない `--ignore-user-config` を指定する。保存済み認証はCLIが引き続き使用する。モデルは `gpt-6-astra`、推論強度は `xhigh`、プロバイダーは `openai` を明示する。

企画は標準入力で渡し、パスは `ArgumentList` に渡す。`workspace-write` と承認拒否を明示し、生成中のシェルのネットワークアクセスを無効にする。シェルへ引き継ぐ環境変数を基本項目に限定し、KEY・SECRET・TOKENを含む名前の除外も有効にする。モデルへの接続はCLIが行うため、インターネット接続は必要。

サンドボックスはOS・Codex CLIの実装に依存する。ファイル読み取りの完全隔離や、AIが作ったコードの安全性を保証するものではない。CLI実行ファイルには自身でインストールした信頼できるものを指定し、生成コードを実行する前に内容を確認する。アプリは生成したUnity/Avaloniaプロジェクトを自動実行しない。

## 改善用コピー

改善は生成済みゲームのコピーへ適用し、アプリは元のファイルへ書き込みません。元フォルダ配下への出力は別名パス経由も拒否します。コピー対象のシンボリックリンク・reparse pointは拒否し、最大10,000ファイル・256MiBに制限します。コピー中の取消も元ファイルを変更しません。コピー中に元ゲームを別のアプリで編集する運用は想定しません。

隠しファイル・隠しフォルダ（`.git`、`.codex`、`.env*`など）、既知の認証ファイル（`auth.json`、`credentials.json`、`secrets.json`）、秘密鍵（`.pem`、`.key`、`.p12`、`.pfx`）、`*.secret.*`、実行ログ、自動保存・復旧データ、依存・ビルドキャッシュ（`node_modules`、`bin`、`obj`、Unityの`Library`・`Temp`・`Logs`・`UserSettings`）はコピーしません。ファイル名による除外なので、任意のソースや素材内に埋め込まれた秘密値は判別できません。機密情報を含むゲームを改善対象にしないでください。

前回の企画・説明・完了レポートを `baseline-*` に分け、新しい補完企画と完了レポートを要求します。未知項目の保持はCLI実行前に読み込んだ企画と照合します。

## 公開に含めない情報

企画本文・感想・改善対象のゲームはモデルへ送信され、ローカルの生成フォルダにも保存される。入力・感想・プロンプト・ログ・履歴には未公開の企画やPCのパスが含まれ得る。共有する場合は対象ファイルの内容を確認する。

`.gitignore` で生成フォルダ `mock-*`、ビルド成果物、ログ、自動保存・復旧データ、Codex設定、認証ファイル、環境設定、秘密鍵を除外する。任意名で書き出した企画JSONは自動判別できないため、個別に確認する。`.gitignore` は既に追跡されたファイルや強制追加を防ぐ仕組みではない。

## リポジトリの保護

- Secret scanningとpush protectionを有効にし、既知の認証情報形式の混入を検知・抑止する。
- Dependabotのアラート・セキュリティ更新を有効にし、NuGetとGitHub Actionsの更新確認を行う。
- CIはGitHubが提供するランナー上のビルド・自動テスト。Codexへのログインや実モデルの生成を行わず、個人PCの認証をCIへ渡さない。
- Actionsの実行権限は `contents: read`、認証の永続化は無効、参照アクションはコミットSHAで固定する。
- NuGetの直接・間接依存を監査し、CIでは監査警告NU1900〜NU1905をエラーとして扱う。
- `develop` はPRとCI `test` を必須とする。デプロイやアプリ配布の処理は設けていない。

## 問題の報告

脆弱性は [GitHubの非公開報告](https://github.com/MasaKoha/Mosaic/security/advisories/new) へ送る。認証情報や未公開の企画を公開Issue・PRへ貼らない。

確認日: 2026-09-12。コードと公開履歴の秘密情報スキャン、NuGetの既知の脆弱性監査を行った。未知の脆弱性やすべてのOSの安全性を保証するものではない。動作確認の範囲は [検証記録](docs/verification.md) を参照。
